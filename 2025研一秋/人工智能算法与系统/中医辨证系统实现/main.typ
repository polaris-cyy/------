#import "template.typ": project, indent
#import "code.typ": *

#show: project.with(
  course: "人工智能算法与系统",
  lab_name: "中医辨证系统实现",
  stu_name: "陈岳阳",
  stu_num: "22521171",
  date: (2025, 11, 20),
  major: "计算机技术",
  department: "计算机科学与技术",
  show_content_figure: true,
  watermark: "ZJU 陈岳阳",
)

= 算法描述

在慢性淋巴细胞白血病中医辨证系统中，核心目标是将患者的症状描述映射到对应的证型与治法。本实验采用大模型推理的方式，通过读取示例数据提供上下文信息，调用大模型生成符合中医辨证规范的输出，实现症状—证型—治法的自动匹配。

== 数据读取与处理

系统首先读取训练示例数据 train_data.csv，提取症状描述、证型和治法信息，用于构建大模型的上下文提示（system prompt），以保证模型生成结果风格与示例数据一致。通过这种方式，系统能够为大模型提供丰富的示例上下文，保证推理输出的规范性和可解释性。

#code.data-process

== 系统提示词构建

系统提示词（system prompt）由三部分组成：

+ 角色定义：指定模型为“经验丰富的中医专家”，保证输出风格符合中医辨证规范。

+ 输出约束：明确要求 JSON 格式输出 {"证型":"", "治法":""}，确保结构化结果便于解析。

+ 示例上下文：附加训练数据示例，使模型生成的结果符合过往问诊风格。

#code.system-prompt

== 大模型调用与推理

在系统提示和用户症状输入的基础上，通过 OpenAI SDK 调用大模型进行推理。使用 temperature=0.3 和 top_p=0.7 控制生成结果的多样性与稳定性。

#code.llm-inference

== 响应解析与输出

系统对大模型返回内容进行解析，提取 JSON 中的 证型 和 治法，并封装为预测函数接口。通过该方法，实现了 症状 → 证型 → 治法 的全流程自动化推理。

#code.response-parse

#pagebreak()

= 算法性能分析

本实验基于大模型推理实现慢性淋巴细胞白血病中医辨证系统，核心性能指标包括 模型调用响应时间、解析效率 和 输出准确性。由于系统依赖远程大模型接口，因此主要性能瓶颈在于模型推理时间和网络通信延迟。系统通过 OpenAI SDK 调用 ernie-4.5-0.3b 大模型进行推理，每次请求包含系统提示和用户症状输入。大模型调用是系统性能瓶颈的核心部分，也是实验中关注的主要对象。

#code.response-time

#pagebreak()

= 研究展望

本实验构建的慢性淋巴细胞白血病中医辨证系统已能实现症状到证型及治法的自动匹配，但仍存在一定局限性，未来研究和优化方向包括以下几个方面：

== 模型调用与推理优化

当前系统使用远程大模型进行推理，单条症状调用延迟约 0.6~1.1 s。未来可考虑：

+ 模型压缩与蒸馏：将大模型蒸馏为轻量级本地模型，以减少网络依赖和延迟。
+ 异步或批量调用：对于多条症状同时处理，可采用异步请求或批量推理策略，提高整体吞吐量。
+ 缓存机制：对常见症状组合引入本地缓存，避免重复请求，提高响应速度。

== 数据增强与知识扩展

目前系统依赖提供的训练示例数据，样本量有限：

+ 增加训练样本：收集更多真实病例数据，丰富症状、证型和治法的覆盖范围。
+ 引入知识图谱：构建中医症状-证型-治法知识图谱，使模型可以结合图结构进行推理，提高可解释性。
+ 数据增强：对症状描述进行语义同义替换或拼写变体扩展，增强模型对自然语言多样性的鲁棒性。

#indent()
综上所述，本实验实现的中医辨证系统在准确性和可解释性方面具备基础性能，但仍有优化空间。通过模型压缩、数据扩展、解释性增强及多任务扩展，未来该系统有望成为可实际应用于临床辅助诊断的智能工具，为慢性淋巴细胞白血病及其他疾病的辨证施治提供科学参考。